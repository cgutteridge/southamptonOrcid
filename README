Clone (usually from git@sourcekettle.ecs.soton.ac.uk:projects/fatfree-project.git)

Then 
git submodule init
git submodule update --recursive

The idea is that by default LDAP auth is not currently enabled because you will want to write a shim which does something with user accounts for each user that logs in. It may be this was a mistake and you should update this project so auth works out of the box.


